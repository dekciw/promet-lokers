import { describe, it, expect } from 'vitest';

// generateNZ() реализуется в Plan 02 (Wave 1).
// Сейчас — заглушка, чтобы зафиксировать формат имени файла (DOC_5).
describe('generateNZ filename format (DOC_5)', () => {
  it.todo('форматирует имя файла как {article}_{YYYY-MM-DD}.pdf — реализуется в Plan 02');
});
